{
  "name": "gastos-extraordinarios",
  "version": "0.4.0-alpha.10",
  "private": true,
  "description": "Control de gastos extraordinarios de pensión de alimentos. Offline-first, sin backend en v1.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/dev-server.js --root=dist",
    "test": "node --test tests/unit/*.test.js tests/unit/shared/*.test.js tests/unit/domain/*.test.js tests/unit/application/*.test.js tests/unit/infrastructure/*.test.js tests/component/*.test.js tests/integration/*.test.js",
    "test:unit": "node --test tests/unit/*.test.js tests/unit/shared/*.test.js tests/unit/domain/*.test.js tests/unit/application/*.test.js tests/unit/infrastructure/*.test.js",
    "test:integration": "node --test tests/integration/*.test.js",
    "coverage": "node --experimental-test-coverage --test tests/unit/*.test.js tests/unit/shared/*.test.js tests/unit/domain/*.test.js tests/unit/application/*.test.js tests/unit/infrastructure/*.test.js tests/component/*.test.js tests/integration/*.test.js",
    "lint": "eslint src tests scripts service-worker.js",
    "lint:fix": "eslint src tests scripts service-worker.js --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "emulators": "firebase emulators:start --only auth,firestore",
    "test:auth-emulator": "firebase emulators:exec --only auth \"node --test tests/integration/emulator/auth-emulator.test.js\"",
    "test:firestore-emulator": "firebase emulators:exec --only auth,firestore \"node --test tests/integration/emulator/firestore-rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "eslint": "^9.9.0",
    "fake-indexeddb": "^6.0.0",
    "firebase": "^12.16.0",
    "firebase-tools": "^15.24.0",
    "husky": "^9.1.5",
    "lint-staged": "^15.2.9",
    "prettier": "^3.3.3",
    "serve": "^14.2.3"
  },
  "dependencies": {},
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{css,html,json,md}": [
      "prettier --write"
    ]
  }
}
